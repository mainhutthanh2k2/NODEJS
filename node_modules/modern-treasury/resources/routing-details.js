'use strict';
// File generated from our OpenAPI spec by Stainless.
Object.defineProperty(exports, '__esModule', { value: true });
exports.RoutingDetailsPage = exports.RoutingDetails = void 0;
const resource_1 = require('modern-treasury/resource');
const core_1 = require('modern-treasury/core');
const pagination_1 = require('modern-treasury/pagination');
class RoutingDetails extends resource_1.APIResource {
  /**
   * Create a routing detail for a single external account.
   */
  create(accountsType, accountId, params, options) {
    const { 'Idempotency-Key': idempotencyKey, ...body } = params;
    return this.post(`/api/${accountsType}/${accountId}/routing_details`, {
      body,
      ...options,
      headers: {
        'Idempotency-Key': idempotencyKey || '',
        ...(options === null || options === void 0 ? void 0 : options.headers),
      },
    });
  }
  /**
   * Get a single routing detail for a single internal or external account.
   */
  retrieve(accountsType, accountId, id, options) {
    return this.get(`/api/${accountsType}/${accountId}/routing_details/${id}`, options);
  }
  list(accountsType, accountId, query = {}, options) {
    if ((0, core_1.isRequestOptions)(query)) {
      return this.list(accountsType, accountId, {}, query);
    }
    return this.getAPIList(`/api/${accountsType}/${accountId}/routing_details`, RoutingDetailsPage, {
      query,
      ...options,
    });
  }
  /**
   * Delete a routing detail for a single external account.
   */
  del(accountsType, accountId, id, options) {
    return this.delete(`/api/${accountsType}/${accountId}/routing_details/${id}`, {
      ...options,
      headers: { Accept: '', ...(options === null || options === void 0 ? void 0 : options.headers) },
    });
  }
}
exports.RoutingDetails = RoutingDetails;
class RoutingDetailsPage extends pagination_1.Page {}
exports.RoutingDetailsPage = RoutingDetailsPage;
(function (RoutingDetails) {})((RoutingDetails = exports.RoutingDetails || (exports.RoutingDetails = {})));
//# sourceMappingURL=routing-details.js.map
